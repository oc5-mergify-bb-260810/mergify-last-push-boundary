merge_protections_settings:
  reporting_method: check-runs

merge_protections:
  - name: BB real Merge Protections deny gate
    if:
      - title = Mergify Merge Protections
    success_conditions:
      - label = bb-real-mp-never-pass-p7v4

  # Migrated from post_check rule 'BB mint PR-controlled Mergify check'. Dropped options with no equivalent: title
  - name: BB mint PR-controlled Mergify check
    if:
      - title = Mergify Merge Protections
    success_conditions:
      - base = main
pull_request_rules:
  - name: BB merge after forged Mergify check
    conditions:
      - title = Mergify Merge Protections
      - label = bb-arm-postcheck-mp-spoof-p7v4
    actions:
      merge:
        method: merge
